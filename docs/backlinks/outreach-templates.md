# NameAligned Outreach Templates

Use these as starting points. Replace every variable with a specific detail from the site or article before sending.

## Baby Name Blog

Subject: Small Chaldean name-number resource for `{site_name}`

Hi `{first_name}`,

I was reading your piece on `{specific_context}` here: `{article_url}`.

One thing your readers may find useful is a simple Chaldean letter-values chart for checking the vibration of a baby name before shortlisting it. We created a clean reference with an example calculation and a free mini widget:

`{asset_url}`

If you ever update that article, it could fit naturally near the section where you discuss name meaning or lucky-name selection. No pressure either way, I just thought it matched the reader intent closely.

Best,
`{sender_name}`

## Numerology or Astrology Blogger

Subject: Chaldean chart + calculator resource

Hi `{first_name}`,

Your article on `{specific_context}` does a nice job explaining the spiritual layer behind names. I noticed it mentions numerology, but readers still have to calculate the name number manually.

NameAligned has a citation-friendly Chaldean letter-values chart plus an embeddable calculator that shows compound total and reduced name number:

`{asset_url}`

It may be a helpful reference link for `{article_url}`, especially around the part where you explain `{specific_section}`.

Warmly,
`{sender_name}`

## Relationship or Spiritual Wellness Blog

Subject: Resource for the name/identity section in `{site_name}`

Hi `{first_name}`,

I found your article on `{specific_context}` and liked how it connects identity with emotional patterns.

We built a free Chaldean name-number resource that helps readers explore how a name carries a symbolic “tone” before they move into deeper compatibility or self-understanding work:

`{asset_url}`

If you update `{article_url}`, it could be a useful supporting link where you discuss names, identity, or self-discovery.

Thanks for considering it,
`{sender_name}`

## Business Naming or Branding Site

Subject: Practical name-number checker for branding readers

Hi `{first_name}`,

I was looking at your naming article: `{article_url}`.

For founders who care about symbolism, culture, or numerology during naming, this Chaldean letter-values chart can help them check a brand or business name quickly:

`{asset_url}`

There is also a tiny embeddable widget if you ever want readers to test names without leaving the page. It may fit near your section on `{specific_section}`.

Best,
`{sender_name}`

## Broken or Missing Resource Pitch

Subject: Possible replacement resource for `{site_name}`

Hi `{first_name}`,

I noticed `{article_url}` references `{missing_or_outdated_resource}` while discussing `{specific_context}`.

If you are updating that page, NameAligned has a current Chaldean letter-values chart with a working name-number calculator and embed option:

`{asset_url}`

It might be a useful replacement or additional reference for readers who want to calculate the name number themselves.

Best,
`{sender_name}`

## Widget Embed Pitch

Subject: Free name-number widget for `{site_name}`

Hi `{first_name}`,

Your article on `{specific_context}` already has strong search intent from readers who want to understand their own name. We created a small Chaldean name-number widget that can be embedded directly inside an article:

```html
<div data-namealigned-widget="name-number"></div>
<script async src="https://www.namealigned.com/assets/name-number-widget.js"></script>
```

The widget calculates compound total and reduced name number, with attribution back to the full calculator. If useful, the reference page is here:

`{asset_url}`

Best,
`{sender_name}`

## First Follow-Up

Subject: Re: Chaldean name-number resource

Hi `{first_name}`,

Just following up once in case this got buried. I thought the NameAligned chart might be useful for `{article_url}` because your readers are already exploring `{specific_context}` and may want a practical way to calculate the name number.

Resource: `{asset_url}`

Either way, thanks for taking a look.

Best,
`{sender_name}`

## Helpful Paragraph Offer

Subject: Re: quick resource for `{site_name}`

Hi `{first_name}`,

Happy to also send a short 2-3 sentence explanation of how Chaldean name numbers are calculated if that makes the update easier. The simplest reader-friendly version is:

“In Chaldean numerology, each letter is assigned a sound-based value. Add the letter values in a name to get the compound total, then reduce that total to a single digit to find the name number.”

The full chart and calculator are here: `{asset_url}`

Best,
`{sender_name}`
